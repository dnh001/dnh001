name = "dnh001-pages"
compatibility_date = "2024-06-20"

[builds]
  command = "rm -rf node_modules package-lock.json && npm cache clean --force && npm install --progress=false && npm run build:h5"
  directory = "dist"
