{
  "name": "dnh001-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev:local": "miniflare --watch"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "hono": "^4.4.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "wrangler": "^3.60.0",
    "miniflare": "^3.0.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
